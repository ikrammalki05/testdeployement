# name: Backend CD - Azure Deploy

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'backend/**'
#       - '.github/workflows/backend-cd.yaml'

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1. Checkout
#       - name: Checkout code
#         uses: actions/checkout@v6.0.1

#       # 2. Login Azure (utilise un secret AZURE_CREDENTIALS)
#       - name: Azure login
#         uses: Azure/login@v2.3.0
#         with:
#           creds: ${{ secrets.AZURE_CREDENTIALS }}

#       # 3. Login Container Registry (ACR)
#       - name: Login to Azure Container Registry
#         uses: docker/login-action@v3.6.0
#         with:
#           registry: ${{ secrets.ACR_LOGIN_SERVER }}
#           username: ${{ secrets.ACR_USERNAME }}
#           password: ${{ secrets.ACR_PASSWORD }}

#       # 4. Build & Push Docker image
#       - name: Build and push backend Docker image
#         uses: docker/build-push-action@v6.18.0
#         with:
#           context: ./backend
#           file: ./backend/Dockerfile
#           push: true
#           tags: |
#             ${{ secrets.ACR_LOGIN_SERVER }}/backend:${{ github.sha }}
#             ${{ secrets.ACR_LOGIN_SERVER }}/backend:latest

#       # 5. Déploiement sur Azure Web App (conteneur)
#       - name: Deploy to Azure Web App
#         uses: Azure/webapps-deploy@v2.2.17
#         with:
#           app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
#           images: ${{ secrets.ACR_LOGIN_SERVER }}/backend:latest

# backend.cd.yaml (minimal pour test)
name: CD Backend Placeholder

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Docker Image
        run: docker pull backend
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          images: backend-placeholder
